<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Cart</title>
    <style>
        th{
            text-align: left;
            padding: 0 40px;
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    <div class="flex w-full h-screen bg-blue-200" id="wrapper">
        <div class="w-1/3 h-full bg-red-100 flex justify-center items-top pt-5" id="left">
            <div class="w-[90%] h-[75%] rounded-md overflow-hidden" id="box">
                <div class="w-full h-[75%] bg-[<%= user.cart[0].bgcolor %>]" id="img-con">
                    <img src="data:image/jpeg;base64, <%= user.cart[0].image.toString('base64') %>" alt="">
                </div>
                <div class="w-full h-[15%] bg-[<%= user.cart[0].panelcolor %>] flex items-center px-5 text-xl font-medium text-[<%= user.cart[0].textcolor %>]" id="productname">
                    <%= user.cart[0].productname %>
                </div>
                <div id="total" class="w-full h-[10%] bg-blue-600 flex justify-between items-center px-5 font-bold text-[<%= user.cart[0].textcolor %>]">
                    <div id="left">Net Total</div>
                    <div id="right"><%= user.cart[0].price %></div>
                </div>
            </div>
        </div>
        <div id="right" class="w-2/3 h-full pt-32 pl-16">
            <h1 class="text-2xl font-medium text-black">Price Breakdown</h1><br>
            <table>
                <tr>
                    <th>Total MRP</th>
                    <td>Rs.<%= user.cart[0].price %></td>
                </tr>
                <tr>
                    <th>Discount on MRP</th>
                    <td class="text-red-700">-<%= user.cart[0].discount %>%</td>
                </tr>
                <tr>
                    <th>Platform Fee</th>
                    <td>Rs.0</td>
                </tr>
                <tr>
                    <th>Shipping Charge</th>
                    <td>FREE</td>
                </tr>
            </table>
            <div id="line" class="w-[80%] bg-black h-[1px] mt-7"></div>
            <table>
                <tr>
                    <th>Total Amount</th>
                    <td class="text-green-600 font-bold">Rs.<%= user.cart[0].price - (user.cart[0].price * user.cart[0].discount / 100) %></td>
                </tr>
            </table>
        </div>
    </div>
</body>
</html>